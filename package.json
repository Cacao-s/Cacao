{
  "name": "cacao",
  "private": true,
  "workspaces": [
    "apps/*",
    "shared/*"
  ],
  "scripts": {
    "dev:api": "cd server && go run ./cmd/api",
    "dev:jobs": "cd server && go run ./cmd/jobs",
    "dev:mobile": "npm --prefix apps/mobile run start",
    "lint": "npm --prefix apps/mobile run lint || true",
    "typecheck": "npm --prefix apps/mobile run typecheck || true"
  }
}